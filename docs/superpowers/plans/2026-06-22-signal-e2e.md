# Signal/E2E Implementation Plan (#3)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Signal 1:1 (Double Ratchet + X3DH) do zero, validado por golden vectors da libsignal da Baileys, mais upload de prekeys e recebimento+decifragem de uma mensagem real.

**Architecture:** Novo pacote `internal/signal` (ratchet, session, x3dh, message format). `internal/store` ganha SignalStore real. `internal/client` ganha upload de prekeys e handler de `<message>`. De-risco: `testdata/signal/session_ab.json` (Baileys libsignal) + live.

**Tech Stack:** Go 1.22, x/crypto (hkdf, curve25519), protobuf (libsignal WhisperTextProtocol + WAProto.Message). Deps pinadas inalteradas.

## Global Constraints
- Não fazer upgrade das deps pinadas (go 1.22).
- Validação por decifrar/reproduzir ciphertexts reais, não por igualar blob de sessão do libsignal.
- TDD; commits frequentes. Nada sensível no git (creds/sessões do chip gitignored).

---

### Task 1: Harness — golden vectors `session_ab.json`
**Files:** Create `harness/gen_signal_vectors.mjs`; gera `testdata/signal/session_ab.json`.
- [ ] Achar a libsignal que a Baileys usa (procurar em node_modules; provavelmente `libsignal` com `SessionBuilder`/`SessionCipher`/`KeyHelper`).
- [ ] alice/bob: gerar identities, regIds; bob gera signedPreKey + one-time preKey (bundle). alice `SessionBuilder.processPreKey(bundle)`, cifra msg1 (pkmsg). bob processa+decifra. Trocar msg2 (alice→bob, agora "msg"), msg3 (bob→alice) p/ exercitar DH ratchet.
- [ ] Dump JSON: todas as chaves (priv/pub hex), bundle, e cada troca `{dir, type, ciphertextHex, plaintextHex}`.
- [ ] Rodar offline; confirmar não-vazio e que a própria Baileys round-trips.

### Task 2: signal/message — formato SignalMessage + PreKeySignalMessage
**Files:** Create `internal/signal/message.go`, `message_test.go`; protobuf do libsignal.
- [ ] Gerar/hand-encode `SignalMessage{ratchetKey,counter,previousCounter,ciphertext}` e `PreKeySignalMessage{registrationId,preKeyId,signedPreKeyId,baseKey,identityKey,message}` (WhisperTextProtocol.proto do libsignal). CONFIRMAR números de campo.
- [ ] Version byte (`(curr<<4)|prev`, =0x33 p/ v3) e MAC de 8 bytes (HMAC-SHA256 sobre version+serialized com macKey; truncado).
- [ ] Teste: parsear os ciphertexts de `session_ab.json` (separar version/body/MAC); campos batem com o esperado.

### Task 3: signal/ratchet — KDFs
**Files:** Create `internal/signal/ratchet.go`, `ratchet_test.go`.
- [ ] rootKDF (HKDF "WhisperRatchet"), chainKey→messageKeys (HMAC + HKDF "WhisperMessageKeys"), chainKey next.
- [ ] Teste contra os valores derivados em `session_ab.json` (chaves de cadeia/mensagem expostas), ou contra vetores conhecidos.

### Task 4: signal/session — SessionCipher.Decrypt + X3DH responder
**Files:** Create `internal/signal/session.go`, `x3dh.go`, `session_decrypt_test.go`.
**Interfaces:** Produces `SessionCipher`, `ProcessPreKeyMessage`, `SessionRecord` (serializável).
- [ ] X3DH responder: derivar root key inicial dos DHs (IK_a/EK_a com IK_b/SPK_b/OPK_b — ordem/infos do libsignal).
- [ ] Decrypt pkmsg e msg de `session_ab.json` → plaintext bate. DH ratchet ao receber nova ratchetKey.
- [ ] Verificação de MAC obrigatória.

### Task 5: signal/session — Encrypt + X3DH initiator (reproduzir golden)
**Files:** `internal/signal/session.go` (+), `session_encrypt_test.go`.
- [ ] X3DH initiator a partir do bundle; Encrypt.
- [ ] Teste: dado o mesmo estado do golden, reproduzir os ciphertexts determinísticos (ou, onde houver aleatoriedade de ephemeral, injetar a do vetor).

### Task 6: store — SignalStore real
**Files:** Modify `internal/store/sqlite.go`, `internal/store/store.go`; `signal_store_test.go`.
- [ ] Implementar LoadSession/StoreSession, prekeys (load/remove/store batch), signed prekey, identities — serializando `SessionRecord` e chaves no `signal_kv`.
- [ ] Teste round-trip de cada.

### Task 7: client — prekey gen + upload
**Files:** `internal/keys/keys.go` (GenPreKeys), `internal/client/prekeys.go`, test.
- [ ] GenPreKeys(start,count). Montar o nó de upload (xmlns=encrypt: registration/type/identity/list/skey) conforme Baileys getNextPreKeysNode. Persistir no store.
- [ ] Teste: o nó montado tem a estrutura/contagem certa (comparar campos com o formato Baileys; ids e chaves presentes).

### Task 8: client — recebimento + receipt
**Files:** `internal/client/receive.go`, test.
- [ ] Pós-login: enviar o que o WA exige p/ fluxo (presença/iq inicial mínimos).
- [ ] Handler `<message>`: from, `<enc type>`, decifrar (pkmsg/msg via SessionCipher+store), unpad, `WAProto.Message.Unmarshal`, extrair texto. Emitir `MessageEvent{From,Text,ID}`. Enviar `<receipt>`/ack.
- [ ] Teste offline com node sintético montado a partir de `session_ab.json` (enc bytes) → MessageEvent com o texto certo.

### Task 9: live — receber mensagem real
**Files:** `internal/client/live_recv_test.go` (`//go:build live`) e/ou `cmd/wa-pair` ganha modo `-listen`.
- [ ] Com as creds do chip: login → upload prekeys → aguardar mensagem; imprimir o texto decifrado. Rodado manualmente.

## Definition of Done (#3)
- `go test ./...` verde incl. vetores diferenciais do Signal.
- Decifrar + reproduzir (determinístico) os ciphertexts do `session_ab.json`.
- Store Signal round-trip; upload de prekeys no formato Baileys.
- **Live:** receber e decifrar uma mensagem de texto real no chip + receipt.
